/*  export default function decorate(block) {
const ctaBtn = document.createElement('Button');
  const itm = [...block.children][0];
  const itmProps = [...itm.children][0];

  var myLink = [...itmProps.children][0];
  var linkIcon = [...itmProps.children][1];
  var ctaIcon = document.createElement('span');
  
  myLink.setAttribute('target', '_blank'); 
  [...block.children].forEach((row) => {
    var btnProps = [...row.children];
    [...row.children].forEach((div) => {
      //div.className = div.children[0].innerHTML;
      //"cta-prop";
      //console.log(div.children[0].innerHTML);
    });
    ctaBtn.className = 'button';
    ctaBtn.innerHTML = btnProps[0].children[0].innerHTML;
    //var linkTxt = btnProps[0].children[3].innerHTML;
    ctaBtn.setAttribute('href', 'stgwww.capella.edu');
    //ctaBtn.setAttribute('data-link', linkTxt);
    // + btnProps[1].children[0].innerHTML;
    //ctaBtn.setAttribute('target', btnProps[0].children[2].innerHTML);
    //ctaBtn.setAttribute('href', btnProps[0].children[3].innerHTML);
  }); */
  /*ul.querySelectorAll('picture > img').forEach((img) => img.closest('picture').replaceWith(createOptimizedPicture(img.src, img.alt, false, [{ width: '750' }])));
  //block.textContent = '';
  //console.log(btnProps);
  //block.append(myLink);
} */